/**
 * Button sets for `MessageBoxDialog`, matching the Windows Forms
 * `MessageBoxButtons` enum (names and values).
 */
export enum MessageBoxButtons {
  OK = 0,
  OKCancel = 1,
  AbortRetryIgnore = 2,
  YesNoCancel = 3,
  YesNo = 4,
  RetryCancel = 5,
  CancelTryContinue = 6
}
